.goal02
	background-color $white
	.container
		min-height: 640px;
		flex-direction column
		&:before
			content: '02';
			color #f5f5f5
			font-weight bold
			position: absolute;
			bottom: 0;
			right: -4rem;
			font-size: 40rem;
			
		.columns
			display flex
			.left
				flex 1 0 25%
				min-width 240px
			.right
				ul
					display flex
					flex-wrap wrap
					li
						flex: 0 0 33%;
						max-width: 250px;
						margin: 0 0 5rem 0;
						header
							color: $darkgrey
						.inner
							color: $white
